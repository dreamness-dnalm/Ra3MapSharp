# 2026-09-15 代码恢复记录

## 基线与证据

- 恢复分支：`codex/recover-backup-code`。
- 起点：从远程 `main` 拉取的 `main/main`，提交 `8eeb17b`。
- 备份目录：`H:\backup`，三个 DLL 的文件时间为 2026-09-14 19:53。
- DLL 的 AssemblyInformationalVersion 均记录 `1.0.0+8dd7afa81822d7db80b3d9b3ae5f2088f42caa21`。该值只说明构建时的提交基线，不能代表未提交源码的内容。
- 使用 ILSpy 9.1.0.7988 反编译备份和本次 Release 构建结果，逐类型对比。
- 通过 `git fsck --no-reflogs --unreachable` 找回尚未清理的原始源码对象。恢复优先采用原始源码，保留注释和命名；没有用反编译输出整体覆盖项目。

| DLL | SHA-256 |
| --- | --- |
| Facade | `B83D3854C97C98A5B184F3E403C25D054AD8EADB46D9CDAD1A81E81C784CC3E8` |
| Parser | `527A8E68B3798EF1A98B66461C329557C06C59D890B47B16DAF63A60E309AEC5` |
| Transform | `21158613C1433842ADD3AD8E59AC67617CB66290A3F35A94AD11C726F7AC0804` |

## 恢复内容

- 道路对象：`RoadOptions`、`RoadObjectWrap`、`ObjectAsset.OfRoad/IsRoad`、对象分类查询和 Facade 道路增删接口。
- 路径点：九种 `WaypointType`、`wp_` 名称转换、强类型属性、默认 Normal 类型。
- 对象列表：允许重复路径点名称，正确递增路径点 ID，维护普通对象唯一 ID 集合。
- Facade 删除：按包装器持有的对象实例删除，避免同名路径点误删。
- SCB 保存：规范化绝对路径，校验输出路径，创建目录，并在成功 SaveAs 后记录来源路径。
- 恢复七个回归测试文件，以及两个本地手动验证文件 `WpTest.cs`、`UnitTest9.cs`。

### 原始源码对象

下表路径均相对于 `src/`。

| 路径 | Git blob |
| --- | --- |
| Dreamness.RA3.Map.Parser/Asset/Impl/GameObject/ObjectAsset.cs | `a2e670137d9897d6e064f0d1b0a6faa976dc55c3` |
| Dreamness.RA3.Map.Parser/Asset/Impl/GameObject/ObjectsListAsset.cs | `d01cff93f99b7bcf44a2ace089b46a525c505044` |
| Dreamness.RA3.Map.Parser/Asset/Impl/GameObject/ObjectWrap.cs | `844a7c2e76fa6cf5db62ae59dba064990cde86c5` |
| Dreamness.RA3.Map.Parser/Asset/Impl/GameObject/RoadObjectWrap.cs | `525b0cb67e1772e14a359643239a8ae1ff76f90a` |
| Dreamness.RA3.Map.Parser/Asset/Impl/GameObject/RoadOptions.cs | `69147b17f7143aaf43797bf31717207460eaa5bb` |
| Dreamness.RA3.Map.Parser/Asset/Impl/GameObject/WaypointType.cs | `a5bd394d62df99533cfed4a6d0b4ee01b5721418` |
| Dreamness.RA3.Map.Parser/Asset/Impl/GameObject/WaypointWrap.cs | `43d461af797ab0bccb91731184d046cfcd39201d` |
| Dreamness.RA3.Map.Parser/Core/MapScb/Ra3MapScb.cs | `baad6f00dfb5bfaaa0414317a9e0ab1ac316729f` |
| Dreamness.RA3.Map.Facade/Core/Ra3MapFacade/ObjectPart.cs | `b6296f287ab48380febb664854fcfc228ad1b8f9` |
| Dreamness.RA3.Map.Facade/Core/Ra3MapFacade/RoadPart.cs | `0ca483048b64353f03b3194193b51612fc5911ee` |

测试来源：`ObjectClassificationTests`、`DuplicateWaypointTests`、`WaypointTypeTests`、`Ra3MapScbSaveTests`、`WaypointSymmetryTests` 来自本地不可达 blob；道路 Facade/Transform 测试来自历史提交 `b8af298`。手动测试恢复自 `b1adc9dc5cc9e0c18341abbf274f81c0cb18c172` 和 `c32f6f2e65c2346f3606a1a39f133e3ad75134df`，备份不包含测试 DLL，无法据其确认手动测试的最后版本。

## 对比结论与保留差异

- 恢复后的道路、路径点、SCB 类型反编译代码与备份匹配，忽略 XML 注释。
- Facade、Transform 的反编译业务代码与备份一致。Transform 源码无需修改；缺失的 Parser 道路类型恢复后，原有引用即可编译。
- Parser 两份嵌入脚本声明 JSON 与备份字节一致。
- 保留主分支已有的 `ScriptArgument` 原始字节保存、脚本解析 dirty 标记和 ScriptList/ScriptGroup 观察者传播修复。
- 保留主分支已有的 ClipboardFilePath、Save/SaveAs；备份没有这些接口。
- 其余差异为 XML 注释、程序集版本/引用路径，以及等价的布尔转换、位运算和字符串插值反编译表现。
- 本次目标是合并恢复备份中缺失的功能，因此不追求生成与备份逐字节相同的 DLL。

## 验证

- `dotnet build src/Dreamness.RA3.Map.Transform/Dreamness.RA3.Map.Transform.csproj -c Release --no-restore`：通过，包含 Parser、Facade、Transform；仍有原有 nullable 等警告。
- Parser 全量测试：37/37 通过。
- Lua 全量测试：1/1 通过。
- Facade `RoadAbstractionTests`：1/1 通过，包含压缩地图保存再打开。
- Transform `WaypointSymmetryTests`、`RoadTransformTests`：3/3 通过。
- 没有执行依赖本机地图或会生成手动地图的完整 Facade/Transform 测试集。恢复的 `WpTest` 读取 `wp_test`，`UnitTest9` 会写出 `u_053` 地图，均未运行。

反编译输出、差异文件、源码对象映射和构建/测试日志保存在 `H:\ra3-recovery-work`。原始 `H:\backup` DLL 未修改。恢复范围受这三个 DLL 和仍留存的 Git 对象限制，无法据此证明从未编译或写入 Git 的其他内容也已找回。
